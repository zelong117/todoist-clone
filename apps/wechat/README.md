# TaskFlow WeChat Mini Program

Import this directory with WeChat Developer Tools. Before previewing on a device, replace the placeholder API domain in `utils/config.js` with an approved HTTPS domain and configure it in the Mini Program backend.

The scaffold uses the same authenticated REST API boundary as the web app. It never includes server keys or payment secrets.
