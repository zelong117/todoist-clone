[package]
name = "taskflow-desktop"
version = "0.1.0"
description = "TaskFlow desktop application"
authors = ["TaskFlow"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
