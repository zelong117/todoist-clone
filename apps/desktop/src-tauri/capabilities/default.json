{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum desktop permissions for the TaskFlow web application.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
